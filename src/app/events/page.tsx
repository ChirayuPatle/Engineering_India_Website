import { EventCard } from "@/features/event/component/eventCard";

const page = () => {
  return (
    <main className="w-full min-h-screen pt-[6rem] ">
      <EventCard />
    </main>
  );
};

export default page;
